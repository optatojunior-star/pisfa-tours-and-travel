<x-app-layout>
    <x-slot name="header"><div><a href="{{ route('portal.car-hire-bookings.show',$booking) }}" class="text-sm font-bold text-emerald-800 underline">Back to hire request</a><h1 class="mt-3 text-2xl font-bold text-slate-950">Car-hire contract</h1><p class="mt-1 font-mono text-xs text-slate-500">{{ $contract->contract_number }} · Version {{ $contract->version }}</p></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8">@if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-900" role="status">{{session('success')}}</div>@endif<div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-10"><div class="flex flex-col gap-3 border-b border-slate-200 pb-6 sm:flex-row sm:items-start sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-wide text-emerald-700">Issued {{ $contract->issued_at->timezone(config('pisfa.business_timezone'))->format('j M Y, H:i') }}</p><h2 class="mt-2 text-2xl font-black text-emerald-950">{{ $booking->vehicle_name_snapshot }}</h2><p class="mt-1 text-sm text-slate-600">Booking {{ $booking->reference }}</p></div><div class="flex flex-col items-start gap-3 sm:items-end"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $contract->isVoided() ? 'bg-rose-50 text-rose-800' : ($contract->isAccepted() ? 'bg-emerald-50 text-emerald-800' : 'bg-amber-50 text-amber-800') }}">{{ $contract->isVoided() ? 'Voided' : ($contract->isAccepted() ? 'Accepted' : 'Awaiting acceptance') }}</span><a href="{{ route('portal.car-hire-bookings.contracts.download',[$booking,$contract]) }}" class="inline-flex min-h-11 items-center justify-center rounded-xl border border-emerald-200 px-4 text-sm font-bold text-emerald-800 hover:bg-emerald-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600">Download PDF</a></div></div><div class="mt-8 whitespace-pre-line text-sm leading-7 text-slate-700">{{ $contract->terms_snapshot }}</div>@if($contract->isAccepted())<div class="mt-8 rounded-2xl bg-emerald-50 p-5 text-sm text-emerald-900"><strong>Acceptance recorded</strong><p class="mt-1">{{ $contract->accepted_at->timezone(config('pisfa.business_timezone'))->format('l, j F Y \a\t H:i') }} against this exact version.</p></div>@elseif(!$contract->isVoided())<form method="POST" action="{{ route('portal.car-hire-bookings.contracts.accept',[$booking,$contract]) }}" class="mt-8 rounded-2xl border border-amber-200 bg-amber-50 p-5">@csrf @method('PATCH')<label class="flex gap-3 text-sm leading-6 text-amber-950"><input type="checkbox" name="accept_contract" value="1" required class="mt-1 rounded border-amber-400 text-emerald-700"><span>I have read and accept this contract version and understand acceptance will be recorded with security metadata.</span></label><button class="mt-5 min-h-11 rounded-xl bg-emerald-700 px-5 text-sm font-bold text-white">Accept this contract</button></form>@endif</div></div></div>
</x-app-layout>
